export const opponents = {
  CPU: 'cpu',
  PLAYER: 'player',
};

export enum Marks {
  X = 'X',
  O = '0',
}

export enum GameStates {
  NOT_STARTED = 'not_Started',
  INPROGRESS = 'in_progress',
  GAME_OVER = 'game_over',
}
